// Step 2: Define the Car class that extends Vehicle
class Car extends Vehicle {
    // Constructor
    public Car(String name, int capacity) {
        super(name, capacity); // Call the constructor of the Vehicle class
    }
}
